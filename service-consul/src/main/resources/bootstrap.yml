shit_code:
  consul:
    config:
      local:
        path: D:\dev\code\hodgepodge-config
        suffix: yml
    request:
      url: localhost
      port: 8500
spring:
  application:
    name: service-consul
  profiles:
    active: dev
logging:
  config: classpath:logback-spring.xml
  file:
    path: D:/dev/log

