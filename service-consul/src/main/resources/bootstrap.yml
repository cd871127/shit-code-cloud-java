shit_code:
  consul:
    config:
      env: ${spring.profiles.active}
      local:
        path: D:\dev\code\shit-code-cloud-java\app-config
        suffix: yml
      git: 1
      database: 2
spring:
  application:
    name: service-consul
  profiles:
    active: dev
logging:
  config: classpath:logback-spring.xml
  file:
    path: D:/dev/log

