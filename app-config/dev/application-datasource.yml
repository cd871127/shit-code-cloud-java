spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ${shitcode.env.mysql.password}
    username: ${shitcode.env.mysql.username}
    url: jdbc:mysql://${shitcode.env.mysql.host}:${shitcode.env.mysql.port}?useUnicode=true&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=CONVERT_TO_NULL&serverTimezone=UTC&allowPublicKeyRetrieval=true
