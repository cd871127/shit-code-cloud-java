shitcode:
  env:
    consul:
      host: localhost
      port: 8500
    server:
      port: 8800
    rabbitmq:
      addresses: localhost
      port: 5672
      username: guest
      password: guest
    mysql:
      host: localhost
    redis:
      host: localhost
    zipkin:
      host: localhost
      port: 9411
    nginx:
      host: localhost
spring:
  output:
    ansi:
      enabled: always
